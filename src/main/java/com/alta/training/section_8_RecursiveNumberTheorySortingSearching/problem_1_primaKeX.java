package com.alta.training.section_8_RecursiveNumberTheorySortingSearching;

import java.util.ArrayList;
import java.util.List;

public class problem_1_primaKeX {
    public static void main(String[] args) {
        primeMethod(3);
    }

    private static void primeMethod(int x) {
        // x = jumlah data yang ada di list atau panjang list.
        // mencari bilangan prima
        // ketemu masukan ke dalam list
        // tampilkan data dalam list index ke x.

        List<Integer> listPrima = new ArrayList<>();

        boolean prima = false;
        if (x == 2){
            prima = true;
        }
        else
            for (int i = 2; i < x; i++){
                //if (x)
            }





        int pembagi = 2;
        int bill = 2;
        if ( x < 2){
            System.out.println("Bukan Prima");
        }
        else {
            //perulangan sebanyak x, nilai prima disimpan ke list.
            //x digunakan untuk indekx perulangan, bukan nialinya.
//            for (int i = 0; i < x; i++){
//                if (primeCheck(i) ==)
//            }


//            for (Integer tampil:listPrima) {
//                tampil.
//            }
        }

    }
}
